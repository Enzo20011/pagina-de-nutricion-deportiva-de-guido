import React from 'react';
import { motion } from 'framer-motion';

interface SkeletonProps {
  className?: string;
  variant?: 'rect' | 'circle' | 'text';
}

export const SkeletonAdvanced = () => {
  return null;
};

export const SkeletonDashboard = () => {
  return null;
};
